import React , {useState} from 'react'
// use state is a named import so we must must must must have the curly braces 
function UseStateBasic() {
    console.log(useState);
    return (
        <div>
            
        </div>
    )
}

export default UseStateBasic
